using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     Debug.Log("Hello, world!");
        Debug.LogWarning("Hello, I am a warning!");
        Debug.LogError("Player Error!!");
        //Author Maharshi Rawal
        /*A game idea I have is a horror survival game of the player being trapped in school at night while a monster lurks the entire school and you have to escape and I would want to basically learn ai movement and setting up the environment.
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
